---
- name: Git clone fzf
  ansible.builtin.git:
    repo: https://github.com/junegunn/fzf.git
    dest: '{{ git_dir }}/fzf'
    update: true


- name: Configure fzf
  ansible.builtin.shell: |
    set -o pipefail
    echo -e 'n\ny\ny\n' | ./install
  args:
    chdir: '{{ git_dir }}/fzf'
  changed_when: true
